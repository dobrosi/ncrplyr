ncore:
  type: all_own # all_own, xvid, hd, xvid_hun, hd_hun
downloadsPath: ${user.home}/.config/ncrplyr/downloads
waitingTime: 3s
torrentLimit: 1
host: http://localhost:8080

spring:
  thymeleaf: # Thymeleaf
    cache: false
